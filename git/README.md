# Git Target

The git target will ensure the `git` binary is installed. This is a useful target for unattended installations such
as CI pipelines or Docker images

## Dependencies

You should ensure that you have a `$DEFAULT_CODE_DIR` environment variable set, which by default is generated during
`caifs add bootstrap`
