# Homebrew

The Missing Package Manager for macOS (or Linux), originally for macos but also for linux

<https://brew.sh/>

## Notes

The hook scripts are unique in that they intentionally call the `generic` hook after the per-distro
install hooks.
